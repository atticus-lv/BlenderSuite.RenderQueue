schema_version = "1.0.0"

id = "BlenderRenderQueue"
type = "add-on"
version = "0.6.0"

name = "Blender Render Queue"
tagline = "Submit renders and host the BlenderRenderQueue worker"
maintainer = "Atticus"
copyright = ["2025 Atticus"]

license = [
    "SPDX-License-Identifier: Custom",
]

tags = ["Render"]
blender_version_min = "5.0.0"
platforms = ["windows-x64", "macos-arm64", "macos-x64"]

[permissions]
files = "Read and write BlenderRenderQueue data files"
network = "Listen on localhost for the BlenderRenderQueue worker protocol"

[build]
paths_exclude_pattern = [
    "__pycache__/",
    "/.git/",
    "/*.zip",
    "/.gitignore",
    "/.gitmodules",
    "/.idea/",
    "/build/",
]
